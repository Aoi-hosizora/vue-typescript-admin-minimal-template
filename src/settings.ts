interface Settings {
    title: string;
    showSettings: boolean;
    sidebarLogo: boolean;
    fixedHeader: boolean;
}

const settings: Settings = {
    title: 'Vue Typescript Admin',
    showSettings: true,
    fixedHeader: false,
    sidebarLogo: false,
};

export default settings;
